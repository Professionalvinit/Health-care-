import React from 'react'
import ReactDOM from 'react-dom/client'
import HealthPlatform from './app/page.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <HealthPlatform />
  </React.StrictMode>,
)
